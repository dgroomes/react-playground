import React from 'react'
import ReactDOM from 'react-dom/client'
import {RawHtmlToReactExample} from "./RawHtmlToReactExample";

const root = document.getElementById("root");

ReactDOM.createRoot(root).render(<React.StrictMode><RawHtmlToReactExample/></React.StrictMode>);
