/**
 * Naive way to wire in configuration data to the application. Just assign an object to the "window"!
 *
 * (WebStorm auto-generated this '@type' annotation.)
 * @type {{repo: string, user: string}}
 */
window.config = {
    user: "dgroomes",
    repo: "react-playground",
    defaultPage: "README.md"
}